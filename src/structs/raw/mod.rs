//! the `raw` module is for the raw types, which have no resolution performed and
//! contain minimal abstraction over the data itself.

pub mod attribute;
pub mod classfile;
pub mod constant_pool;
pub mod field;
pub mod method;
